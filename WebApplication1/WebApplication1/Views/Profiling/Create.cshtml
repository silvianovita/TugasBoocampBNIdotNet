@model WebApplication1.Models.Profiling

@{
    ViewData["Title"] = "Create";
}

<h1>Create</h1>

<h4>Profiling</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="NIK" class="control-label"></label>
                <select asp-for="NIK" class ="form-control" asp-items="@(new SelectList(@ViewBag.ListofAccount,"NIK","FirstName"))"></select>
            </div>
            <div class="form-group">
                <label asp-for="Education_Id" class="control-label"></label>
                <select asp-for="Education_Id" class ="form-control" asp-items="@(new SelectList(@ViewBag.ListofEducation,"Id", "Degree"))"></select>
            </div>
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
